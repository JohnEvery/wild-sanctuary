spring:
  jpa:
    database: POSTGRESQL
    properties.hibernate.temp.use_jdbc_metadata_defaults: false
    hibernate:
      ddl-auto: create
    properties:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/wildlife_database
    username: wild
    password: root
    driver-class-name: org.postgresql.Driver

server:
  servlet:
    context-path: /wildlife
  port: 9090